# StillLoop App Store Assets

Updated on 2026-05-21 for an App Store metadata-only resubmission. These screenshots replace older generated marketing images with captures based on the current reachable macOS app UI.

## Screenshot Order

1. `screenshots/01-task-setup-2880x1800.png` - Current task setup. Reachable from the main window after onboarding or when starting a new focus.
2. `screenshots/02-focus-running-2880x1800.png` - Active focus session. Enter a task, choose `开始专注`, then let StillLoop collect the current local context.
3. `screenshots/03-permissions-guide-2880x1800.png` - `权限获取引导`. Reachable on first launch or when required Screen Recording or Camera permission is missing.
4. `screenshots/04-focus-review-2880x1800.png` - `专注复盘`. Reachable after ending and reviewing a focus session.
5. `screenshots/05-settings-privacy-2880x1800.png` - `设置` with the `隐私` section. Reachable from the `设置` button.

## App Store Connect Checklist

- Upload the five screenshots in the order above.
- Remove any older App Store screenshots that reference an unavailable permissions page.
- Check App Preview, description, promotional text, release notes, and review notes for stale feature names before resubmitting.
- If an App Preview video still shows outdated UI, replace it or remove it for this metadata-only resubmission.
- Keep App Store screenshots in this directory. Do not use ignored build output directories as the durable source of release metadata.

## Suggested Review Reply

We have replaced the App Store screenshots with images captured from the current app UI. The permissions flow is available during first launch by clicking `开始设置`, or whenever required Screen Recording or Camera permission is missing, and appears as `权限获取引导`. The local processing and privacy details are available in `设置` under the `隐私` section. The outdated permissions screenshot has been removed from the submitted metadata.
